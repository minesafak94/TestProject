Loops helop us repeat instructions in code for example if we have to print hte numbers from 1 to 100 one way
we can do this by copying the code which is bad and not scalable other way of doing this is using loops.

There are 4 Types